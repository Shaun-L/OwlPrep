import { useEffect } from 'react';
import { useLocation } from 'react-router-dom';

function ClearSessionOnNavigate() {
  const location = useLocation();

  

  return null; // Helper component, no UI needed
}

export default ClearSessionOnNavigate;